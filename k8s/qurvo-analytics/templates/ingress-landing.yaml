{{- if .Values.landing.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "qurvo.fullname" . }}-landing
  labels:
    {{- include "qurvo.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.tls.enabled }}
    cert-manager.io/cluster-issuer: {{ .Values.tls.clusterIssuer | quote }}
    {{- end }}
    nginx.ingress.kubernetes.io/proxy-read-timeout: "30"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.className }}
  {{- if .Values.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.domain.landing }}
        - www.{{ .Values.domain.landing }}
      secretName: {{ include "qurvo.fullname" . }}-landing-tls
  {{- end }}
  rules:
    - host: {{ .Values.domain.landing }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "qurvo.fullname" . }}-landing
                port:
                  name: http
{{- end }}
