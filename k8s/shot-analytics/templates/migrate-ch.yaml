{{- if .Values.migrations.ch.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "shot.fullname" . }}-migrate-ch-{{ .Release.Revision }}
  labels:
    {{- include "shot.labels" . | nindent 4 }}
    app.kubernetes.io/component: migrate-ch
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  backoffLimit: 2
  activeDeadlineSeconds: 120
  template:
    metadata:
      labels:
        app.kubernetes.io/component: migrate-ch
    spec:
      restartPolicy: OnFailure
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: migrate-ch
          image: {{ include "shot.image" (dict "svc" .Values.api "root" .) }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          command: ["node", "../../packages/@shot/clickhouse/dist/migrate.js"]
          envFrom:
            - secretRef:
                name: {{ include "shot.fullname" . }}-secret
            - configMapRef:
                name: {{ include "shot.fullname" . }}-config
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
{{- end }}
