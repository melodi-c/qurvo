apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "shot.fullname" . }}-config
  labels:
    {{- include "shot.labels" . | nindent 4 }}
data:
  NODE_ENV: "production"
  CLICKHOUSE_DB: {{ .Values.clickhouse.database | quote }}
