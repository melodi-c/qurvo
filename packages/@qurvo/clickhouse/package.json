{
  "name": "@qurvo/clickhouse",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp -r src/migrations dist/migrations",
    "dev": "tsc --watch",
    "clean": "rm -rf dist .turbo",
    "ch:migrate": "tsx src/migrate.ts",
    "ch:generate": "tsx src/generate.ts"
  },
  "dependencies": {
    "@clickhouse/client": "^1.8.0",
    "@qurvo/ch-query": "workspace:*"
  },
  "devDependencies": {
    "@qurvo/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
