{
  "name": "@shot/clickhouse",
  "version": "0.0.1",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && cp src/migration.sql dist/migration.sql",
    "dev": "tsc --watch",
    "clean": "rm -rf dist .turbo",
    "ch:migrate": "tsx src/migrate.ts"
  },
  "dependencies": {
    "@clickhouse/client": "^1.8.0"
  },
  "devDependencies": {
    "@shot/tsconfig": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
